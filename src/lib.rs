//! Workspace root package for `agbr`.
//!
//! This crate exists so the workspace root is a valid package manifest
//! (release-please's rust strategy rejects virtual workspaces). It has no
//! code; the `agbr` binary lives in `crates/cli`.
